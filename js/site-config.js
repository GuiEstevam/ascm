/** Domínio canónico (sem barra final). Ajustar se o deploy usar outro host. */
export const CANONICAL_ORIGIN = 'https://ascm.com.br';

/** URL completa do WhatsApp (wa.me/…). Substituir pelo número oficial antes do go-live. */
export const WHATSAPP_HREF = 'https://wa.me/5511999999999';
